Task
            From the Linkedin profilke text, extract domain evidence occurrences. Each output item MUST represent ONE verbatim snippet occurrence from the Linkedin profile text.

            Rules
            - Select domains ONLY from the Allowed Domains list.
            - Output 0-N extractions (each extraction = one occurrence/snippet).
            - Do NOT invent domains. If none fits, return an empty list.
            - Each snippet MUST be copied verbatim from the Linkedin profile text (no paraphrase).
            - No duplicate extractions: do not output the same snippet+domain pair twice.
            - Return ONLY valid JSON (no markdown, no extra keys).

            Allowed Domains (choose ONLY from this list):
            {domains_lst}

            Output JSON (Strict)
            {{
            "domains": [
                {{
                "domain": "<exact item from Allowed Domains>",
                "snippet": "<verbatim substring from Linkedin Profile>",
                "source": "Linkedin",
                "snippet_type": "<sentence|line|paragraph|bullet>",
                "confidence": "<high|medium|low>"
                }}
            ],
            "version": {{
                "extraction": "professional_domain_experience_occurrences_v2",
                "builder": "domain_prompt_occurrences_v1"
            }}
            }}

            Empty Case
            {{
            "extractions": [],
            "version": {{
                "extraction": "professional_domain_experience_occurrences_v1",
                "builder": "domain_prompt_occurrences_v1"
            }}
            }}

            Linkedin TEXT:
            {cv_text}